import React from "react";
import PlayArrowIcon from "@material-ui/icons/PlayArrow";

/**
 * States
 */

//not shown, haven't handled the style,
const PhoneRunButton: React.FC = () => {
  return (
    <div>
      <PlayArrowIcon />
    </div>
  );
};

export default PhoneRunButton;

import React from "react";
import { makeStyles } from "@material-ui/core/styles";

interface Props {
  showState: boolean;
}

const Panel: React.FC = () => {
  // console.log(props.showState);
  return <div id="journey-frontend-lib-panel"></div>;
};

export default Panel;
